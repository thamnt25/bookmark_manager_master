# Bookmark Manager

This repository is split into separate application folders:

- `frontend/` contains the existing React + TypeScript + Vite app.
- `backend/` is reserved for backend code.

Run frontend commands from the `frontend/` directory.
